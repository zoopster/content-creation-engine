# Content Creation Engine - Requirements
# Python 3.8+ required

# ===== Core Dependencies (All Phases) =====
pyyaml>=6.0.1              # Workflow definitions and configuration

# ===== API Dependencies =====
fastapi>=0.109.0           # REST API framework
uvicorn[standard]>=0.25.0  # ASGI server
pydantic>=2.5.0            # Data validation
python-multipart>=0.0.6    # Form data handling

# ===== Phase 3: Document Production =====
# Document generation libraries
python-docx>=1.1.0         # Microsoft Word (DOCX) generation
python-pptx>=0.6.23        # PowerPoint (PPTX) generation
reportlab>=4.0.0           # PDF generation with fine control
Pillow>=10.0.0             # Image handling (required by reportlab)

# ===== Development & Testing =====
pytest>=7.4.0              # Testing framework

# ===== Future Phases =====
# Phase 4+: Research & External Integration
# requests>=2.31.0         # HTTP requests for web search
# beautifulsoup4>=4.12.0   # Web scraping
# openai>=1.0.0            # OpenAI API (if using GPT models)
# anthropic>=0.7.0         # Anthropic API (if using Claude models)

# Optional: Enhanced Markdown Processing
# markdown>=3.5.0          # Better markdown parsing (replaces simple regex parser)
# PyPDF2>=3.0.0           # PDF manipulation (if needed for repurposing)
